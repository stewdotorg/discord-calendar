services:
  bot:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./service-account.json:/app/service-account.json:ro
      - bot_data:/app/data

volumes:
  bot_data:
